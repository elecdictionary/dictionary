第一步：
实现背单词功能。（暂时不使用动态链接库）
实现查询生词功能。
数据库：
1.单词库（单词 音标 词性 中文解释 难度/1,2级）
2.每个用户的学习记录文件记录
3.每个用户的用户名 密码

所有外界接口首字母大写。

需求接口：
1.输入用户名判断该用户名是否已经存在
2.给定用户名 在当前目录下创建一个以该用户命名的目录（可能要有跨平台问题“if define unix”）
  并创建 用户名.info 文件记录用户的信息（用户名，密码，。。。）
3.通过传引用的方法，把词典内容拷贝到一个vector上面。

********************************************************************************************************
对于背单词功能：
我需要的接口：
例句：
1.给出某个单词，返回它的原本所有例句（传string给一个vector，每句话末尾不要有回车），这项功能要做出来，能找到例句最好，没有例句也要有功能
2.给出某个单词，返回它的用户自建例句，无自建例句则返回空vector即可
1,2的数据存储方式由你来决定，只需完成接口功能

用户背单词记录：
3.对于每一个用户，记录下用户每天背了哪些单词，是否一次背过，同一个单词连续3次一次背过就视作已经彻底背下来了，不需要再出现。因此，因此我需要你再维护一个用户已经背过了多少单词的接口（int类型 参数为string类型的用户名），以及有多少单词不需要再背的接口（bool类型 参数是string类型的单词）
P.S.我自己对实现这个接口的想法（可以不必按照我的想法设计，只需要你提供接口功能）：
    对于每天的背单词，提供一个创建当日记录接口（参数 一个string类型的vector记录了当日的所有单词），并实时记录下那些单词已经背过了哪些还没背过，每行一个单词和一个数字表示表示记得和不记得（这样还能确保更方便的找出那些单词已经不需要在背了）
   当然也可以单独搞个文件，维护那些单词需要背，哪些不需要背。


